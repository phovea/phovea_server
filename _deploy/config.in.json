{
  "caleydo_core": {
    "baseUrl": "" //the context path to use a trailing slash will be appended, e.g. /test
  },
  "caleydo_server": {
    "debug": false,
    "logging": {
      "handlers": {
        "file": {
          "filename": "logs/caleydo.log"
        }
      }
    }
  }
}
